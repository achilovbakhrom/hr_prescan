port: 7880
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 60000
  use_external_ip: false

keys:
  # Set via LIVEKIT_API_KEY / LIVEKIT_API_SECRET env vars in .env
  # devkey: devsecret  # DO NOT use default keys — generate strong keys per environment

logging:
  level: info

room:
  empty_timeout: 300
  max_participants: 10

webhook:
  urls: []
